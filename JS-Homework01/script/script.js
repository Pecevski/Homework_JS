// Calculate area of Circle

const pi = Math.PI;
let circleRadius = 10;
let areaOfCircle = pi * (circleRadius * circleRadius);

alert(areaOfCircle);


// Calculate the price of  Phones

let numberOfPhones = 30;
let taxRate = 0.05;
let priceBeforeTax = 119.95;
let priceWithTax = priceBeforeTax + (priceBeforeTax * taxRate);
let totalPriceOfPhones = numberOfPhones * priceWithTax;

alert(totalPriceOfPhones);